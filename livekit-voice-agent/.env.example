# ── LiveKit server credentials ────────────────────────────────────────────────
# Get these from https://cloud.livekit.io → your project → Settings → Keys
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=APIxxxxxxxxxxxxxxxxx
LIVEKIT_API_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── STT: AssemblyAI ───────────────────────────────────────────────────────────
# https://www.assemblyai.com/app → API Keys
ASSEMBLYAI_API_KEY=

# ── TTS: Cartesia ─────────────────────────────────────────────────────────────
# https://play.cartesia.ai → API Keys
CARTESIA_API_KEY=

# ── LLM provider ──────────────────────────────────────────────────────────────
# One of: ollama | openai-compatible | anthropic | openai | google | groq
# Default is "ollama" (self-hosted, no API key, your data stays on your machine)
LLM_PROVIDER=ollama

# Optional: override the model for the chosen provider. Defaults per provider:
#   ollama             llama3.1:8b
#   openai-compatible  meta-llama/Llama-3.1-8B-Instruct
#   anthropic          claude-opus-5
#   openai             gpt-5.1
#   google             gemini-2.5-flash
#   groq               llama-3.3-70b-versatile
#LLM_MODEL=

# ── Self-hosted options ───────────────────────────────────────────────────────
# ollama: where your Ollama server listens (default shown)
#OLLAMA_BASE_URL=http://localhost:11434/v1

# openai-compatible: any server speaking the OpenAI API (vLLM, llama.cpp,
# LM Studio, TGI...). LLM_BASE_URL is required, LLM_API_KEY only if your
# server enforces one.
#LLM_BASE_URL=http://localhost:8000/v1
#LLM_API_KEY=

# ── Hosted provider API keys (only for the provider you picked) ──────────────
# https://console.anthropic.com → API Keys
#ANTHROPIC_API_KEY=
# https://platform.openai.com → API Keys
#OPENAI_API_KEY=
# https://aistudio.google.com → Get API key
#GOOGLE_API_KEY=
# https://console.groq.com → API Keys (free tier available)
#GROQ_API_KEY=
